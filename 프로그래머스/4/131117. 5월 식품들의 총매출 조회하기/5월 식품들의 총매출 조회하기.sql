-- 코드를 입력하세요
SELECT P.PRODUCT_ID, P.PRODUCT_NAME, PRICE * SUM TOTAL_PRICE
FROM FOOD_PRODUCT P JOIN (
SELECT PRODUCT_ID, SUM(AMOUNT) SUM
FROM FOOD_ORDER
WHERE PRODUCE_DATE LIKE '2022-05%'
GROUP BY PRODUCT_ID) O
USING(PRODUCT_ID)
ORDER BY TOTAL_PRICE DESC, P.PRODUCT_ID










